<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:w="urn:import:com.yoghurt.crypto.transactions.client.widget" xmlns:g="urn:import:com.google.gwt.user.client.ui">
  <ui:with field="i18n" type="com.yoghurt.crypto.transactions.client.i18n.ApplicationMessages" />
  <ui:with field="res" type="com.yoghurt.crypto.transactions.client.resources.R" />
  <ui:style>
    .content {
    border-radius: 5px;
    background-color: #fff;
    box-shadow: 0px 0px 5px #fff;
    }
    .contentHeading {
    font-size: 31px;
    height: 37px;
    font-weight: bold;
    }
    .linkPanel {
    flex-wrap: wrap;
    align-items: center;
    height: 170px;
    }
    .blockLink {
    width: 140px;
    height: 140px;
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 10px;
    color: white;
    padding: 5px;
    font-size: 18px;
    border: 0px solid white;
    font-weight: bold;
    cursor: pointer;
    transition: border 0.25s ease-out;
    }
    .blockLink:hover {
    border: 10px solid transparent;
    }
    .blockLink:active {
    border-color: black;
    }
    .red {
    background-color: red;
    }
    .red:hover {
    border-color: darkred;
    }
    .blue {
    background-color: blue;
    }
    .blue:hover {
    border-color: darkblue;
    }
    .green {
    background-color: green;
    }
    .green:hover {
    border-color: darkgreen;
    }
    .dance {
    margin-bottom: 10px;
    display: block;
    }
  </ui:style>
  <g:FlowPanel styleName="{style.content}">
    <w:HeadingWidget n="2" text="{i18n.startupGeneral}" />
    <g:InlineHTML hTML="This is an initial hacky and dirty patch/fork of Yoghurt's open source block explorer you can run on top of any node. This version runs on top of the alphad Sidechain Elements sidechain node. Hardly anything works. This deployment will keep up close with the GitHub's head over on:" styleName="{style.dance}" />
    <g:Anchor text="https://github.com/JornC/bitcoin-transaction-explorer/tree/elements" href="https://github.com/JornC/bitcoin-transaction-explorer/tree/elements" styleName="{style.dance}" />
    <g:InlineHTML hTML="The original version of this block explorer (which is also unfinished) runs over on:" styleName="{style.dance}" />
    <g:Anchor text="http://yogh.io" href="http://yogh.io" styleName="{style.dance}" />
    <g:Label text="Please bare with the bugs. I quite literally have no idea what I'm doing or how to interpret anything on these sidechains. I'm in the dark just as much as anyone else. Hope someone enjoys it though." styleName="{style.dance}" />
    <g:InlineHTML hTML="Cheers, /u/yoghurt114" styleName="{style.dance}" />
    
    <g:Label text="Oh right, how to use this explorer: type 'last' in the lookup box above and you'll go to the last block, a block height works aswell. It'll lookup transaction id's, aswell as raw transactions and raw blocks. Just stick it in the lookup box above and it'll try and fetch it for you." styleName="{style.dance}" />
    <g:Label text="Note that I've tested precisely nothing. Literally the only thing I did was 'fix' anything incompatible with sidechains' alphad/elements by commenting away the code that caused an error." styleName="{style.dance}" />
    
    <!-- 
    <g:FlowPanel styleName="{res.css.flex} {style.linkPanel}">
      <g:Label styleName="{style.blockLink} {style.red}" text="{i18n.startupLastBlock}" ui:field="lastBlock" />
      <g:Label styleName="{style.blockLink} {style.blue}" text="Mining simulator" />
      <g:Label styleName="{style.blockLink} {style.green}" text="Contribute" ui:field="p2pkh" />
    </g:FlowPanel>
     -->
  </g:FlowPanel>
</ui:UiBinder>